#include "image_menu.h"
void drawAsciiImage( std::istream& is, std::ostream& os, const Image& image ){
    int row =0;
    for (row, i < width; row++){
        int column=0;
        for (column, i < width; column++){
        
        }
        os<<std::endl;
    }
}
